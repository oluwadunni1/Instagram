[project]
name = "instagram-catalog-pipeline"
version = "0.1.0"
description = "POC: cascade AI pipeline turning a vendor Instagram feed into a structured catalog + attention flags"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = []